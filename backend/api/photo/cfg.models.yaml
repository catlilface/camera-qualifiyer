package: photo
output: internal/pkg/api/photo/models.gen.go
generate:
  models: true
output-options:
  skip-prune: true
  name-normalizer: ToCamelCaseWithInitialisms
  exclude-operation-ids:
    - Health